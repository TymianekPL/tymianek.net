export const host = "http://127.0.0.1/";
export const baseUri = `${host}api/v1/`;
export class URIs {
     static GetToken = `${baseUri}token/account`;
     static GetName = `${baseUri}account/name`;
}
